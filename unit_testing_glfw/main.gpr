with "../glfw/GLFW";

project Main is

	for Languages use ("ada");
	for Source_Dirs use ("src");
	for Main use ("create_window.adb", "get_api.adb", "drop.adb", "test_mouse.adb");
	for Object_Dir use "obj";
	for Library_Dir use "lib";
	for Exec_Dir use "bin";


	package Builder is
	end Builder;

	package Compiler is
	end Compiler;

	package Binder is
	end Binder;

	package Linker is
	end Linker;


end Main;
